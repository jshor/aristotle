{
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn workspace @aristotle/app start",
    "build:app": "yarn workspace @aristotle/app build",
    "build:circuit": "yarn workspace @aristotle/logic-circuit build",
    "build:website": "yarn workspace @aristotle/website build",
    "build": "yarn build:website && yarn build:circuit && yarn build:app"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14",
    "yarn": ">=1.22.0"
  }
}
