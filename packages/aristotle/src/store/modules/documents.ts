const state = [
  // relayedCommand: null,
  // toolboxVisible: false,
  // toolboxSettings: {}
]

const getters = {
}

const mutations = {
}

const actions = {}

export default {
  state,
  getters,
  actions,
  mutations
}
