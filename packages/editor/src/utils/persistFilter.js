export default function persistFilter () {
  // const id = 'cachedFilters'
  // const svg = document.getElementById('cachedFilters')
  // const filters = document.getElementsByTagName('filter')

  // if (!svg && filters.length) {
  //   const newSvg = document.createElement('svg')
  //   newSvg.id = id
  //   newSvg.appendChild(filters[0])

  //   newSvg.style.width = 0
  //   newSvg.style.height = 0
  //   document.body.appendChild(newSvg)
  // }
}