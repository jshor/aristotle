/**
 * Represents a point in 2D Cartesian space (x, y).
 */
export default interface Point {
  /**
   * X-axis coordinate.
   *
   * @type {number}
   */
  x: number

  /**
   * Y-axis coordinate.
   *
   * @type {number}
   */
  y: number
}
